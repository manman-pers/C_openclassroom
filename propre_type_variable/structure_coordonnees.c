#include <Coordonnees.h> /*Inclusion du fichier .h qui contient les protoypes et structures*/

int main(int argc, char *argv[])
{
	/*struct*/ Coordonnees points; /*Creation d'une Variable "point" de type coordonnees, grace au typef dans le .h on peut mettre seulement Coordonnees a la place de struct Coordonnees*/

	point.x = 10; /* pour acceder a chaque composante de la structure  : variable.nom_de_la_composante */
	point.y = 20;

	return 0;
}
